# Alembic Migration Placeholder

This project now uses a migration-ready SQLAlchemy layout with centralized metadata naming conventions and explicit model imports.

Future phases can add:

- `alembic.ini`
- `env.py`
- generated revision scripts inside `alembic/versions/`
